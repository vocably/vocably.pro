{
  "name": "@vocably/extension",
  "version": "0.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "build": "webpack --env production",
    "build:firefox": "TARGET_BROWSER=firefox webpack --env production && sed -i '' \"s/new Function('return this')()/globalThis/g\" dist-firefox/*.js",
    "start": "webpack --watch --env development",
    "start:firefox": "TARGET_BROWSER=firefox webpack --watch --env development",
    "test": "echo '@vocably/extension has no tests.'",
    "extension": "npm start"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/vocably-pro/vocably.git"
  },
  "devDependencies": {
    "string-replace-loader": "^3.3.0"
  }
}
